{
  "dependencies": {
    "redom": "^4.3.0"
  },
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && mv build dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.1.1"
  }
}